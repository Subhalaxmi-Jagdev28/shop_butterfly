name: "Deploy Shop Butterfly"

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install frontend dependencies and build
      working-directory: ./frontend
      run: |
        npm install
        npm run build

    - name: Install backend dependencies
      working-directory: ./backend
      run: |
        pip install pipenv
        pipenv install

    - name: Trigger Render Deploy
      run: |
        curl -X POST https://github.com/Subhalaxmi-Jagdev28/shop_butterfly

    - name: Done
      run: echo "Shop Butterfly build and deploy complete!"